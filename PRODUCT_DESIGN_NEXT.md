# PhotoCleaner 下一版本产品与体验设计

> 文档状态：开发基线草案 1.0  
> 计划周期：下一开发周  
> 当前基线版本：2026.07.12（commit `cd5abea`）  
> 目标平台：Android 11+，重点验证 Android 16 / HyperOS  
> 核心约束：完全离线、无账号、无广告、无照片上传、用户最终确认删除

---

## 1. 文档目的

本文档把 PhotoCleaner 从“照片扫描与删除工具”升级为“完全离线、可解释、低误删的照片体检与整理助手”。它同时作为下一版本的：

- 产品需求文档（PRD）；
- 信息架构和交互设计说明；
- 视觉与内容规范；
- 本地算法和数据结构设计；
- 开发拆分、验收标准和测试计划。

下周开发应优先完成一条可闭环的用户主线，而不是同时扩展大量检测类别：

```text
选择范围 -> 本地体检 -> 查看报告 -> 快速整理 -> 系统确认 -> 结果核验 -> 清理记录
```

---

## 2. 产品判断与定位

### 2.1 不参与的竞争

PhotoCleaner 不与系统相册竞争以下能力：

- 日常照片浏览；
- 图片编辑、滤镜和拼图；
- 云备份和跨设备同步；
- 人物、地点和回忆相册；
- 分享、社交和内容创作；
- 手机缓存清理、加速和通用文件管理。

这些领域需要系统级入口、云基础设施或长期模型投入，不是当前项目的优势。

### 2.2 核心定位

**一句话定位：**

> PhotoCleaner 是一款完全离线的照片体检工具，解释哪些照片值得整理，并让用户以低风险方式快速做决定。

**核心宣传语：**

> 不上传照片，只告诉你为什么值得整理。

**辅助表达：**

- 严格按照你选择的目录检查；
- 每一项建议都有理由；
- 重要照片默认保护；
- 最终删除由系统确认；
- 无网络权限、无账号、无广告 SDK。

### 2.3 差异化支柱

1. **隐私可证明**：不只是宣称离线，而是在产品内展示未申请网络权限、本地分析状态和保存的数据类型。
2. **建议可解释**：用户能看到风险等级、检测理由、对比依据和推荐保留原因。
3. **范围可控制**：支持按目录做专项体检，不把全库扫描伪装成所选范围。
4. **低误删优先**：票据、文档、二维码、近期照片和用户已保留照片默认保护。
5. **快速完成任务**：通过短会话帮助用户处理少量高价值候选，而不是让用户面对几千张照片。
6. **跨品牌一致**：在 Redmi、Samsung、Pixel 等 Android 设备上提供统一的体检和审查逻辑。

---

## 3. 目标用户与核心任务

### 3.1 主要用户

**用户 A：存储空间不足者**

- 相册有数千到数万张照片；
- 系统提示空间不足，但不敢批量删除；
- 希望先处理重复、连拍和旧截图。

**用户 B：隐私敏感者**

- 不希望照片上传到云端分析；
- 对第三方清理工具和广告 SDK 不信任；
- 愿意使用完全离线、开源或行为透明的工具。

**用户 C：定期整理者**

- 希望每周花几分钟整理最近照片；
- 需要明确、可重复的工作流；
- 更看重节省决策时间，而不只是释放空间。

### 3.2 用户要完成的任务（Jobs to be Done）

- 当手机空间不足时，我想先找到低风险候选，快速释放一部分空间，而不是浏览整个相册。
- 当出现一组相似连拍时，我想知道推荐保留哪一张以及为什么，同时保留最终选择权。
- 当工具建议删除照片时，我想知道检测依据和风险，避免误删票据、证件或重要照片。
- 当我只想整理截图或某个应用目录时，我希望扫描数量和结果严格符合所选范围。
- 当我授权删除后，我希望知道实际成功删除了多少张、释放了多少空间，以及是否有失败项。

---

## 4. 产品原则

### 4.1 安全高于释放空间

- 不自动永久删除；
- 不因算法置信度高就绕过用户确认；
- 不将“文档/票据”直接等同于“垃圾”；
- 删除失败或状态不确定时，保留本地记录并提示重试；
- “预计释放”与“实际释放”必须分开显示。

### 4.2 解释高于“AI”标签

- 不使用“AI 精准清理”“一键选出最佳”等过度承诺；
- 使用“推荐保留”“建议检查”“检测到视觉相似”等可验证表达；
- 每条建议至少提供一个用户可理解的原因；
- 置信度不能只显示百分比，必须转换成风险语言。

### 4.3 小任务高于大扫描

- 首页只有一个主要行动：“开始快速整理”或“开始照片体检”；
- 默认会话以候选组数或决策数限制，不以模糊的分钟倒计时强迫用户；
- 支持暂停，退出后保留进度；
- 高级扫描设置逐步展示，不占据首次使用主流程。

### 4.4 本地优先且可证明

- 不增加 `INTERNET` 权限；
- 不增加账号、埋点或远程配置；
- 所有统计默认只保存在设备上；
- 若未来引入崩溃上报，必须单独设计明确的用户选择，不纳入本版本。

---

## 5. 下一版本范围

### 5.1 必须完成（P0）

1. 新版照片体检报告；
2. 风险等级、推荐理由和保护规则；
3. 精确重复照片检测；
4. 快速整理会话；
5. 删除结果与实际释放空间记录；
6. 隐私证明入口；
7. Room 数据迁移和对应测试。

### 5.2 应该完成（P1）

1. 相似照片并排对比；
2. “推荐保留”及推荐依据；
3. 用户手动保护/取消保护；
4. 快速整理进度恢复；
5. 旧截图专项入口。

### 5.3 时间允许再做（P2）

1. 同步缩放查看相似照片细节；
2. 本地偏好统计；
3. 更细的截图内容分类；
4. 每周整理提醒；
5. 清理记录按日期和类别筛选。

### 5.4 本版本不做

- 人脸身份识别；
- 闭眼、笑脸和构图评分；
- OCR 全文索引；
- 云同步和账号；
- 自动后台删除；
- 图片压缩和格式转换；
- 商业广告或订阅系统。

---

## 6. 信息架构

### 6.1 推荐的一级导航

将当前五个一级入口逐步收敛为四个：

| 一级入口 | 目的 | 主要内容 |
|---|---|---|
| 体检 | 了解当前相册状态并发起任务 | 体检报告、扫描范围、主要 CTA |
| 审查 | 完成照片取舍 | 快速整理、分类筛选、相似组对比 |
| 记录 | 建立结果感和信任 | 实际删除数量、释放空间、历史记录 |
| 设置 | 控制规则和隐私 | 保护规则、扫描规则、隐私证明 |

“扫描”不再作为永久一级页面，而是“体检”中的核心操作。若一周内重构导航风险过高，可以暂时保留五个入口，但首页必须以体检报告和单一主要 CTA 为中心。

### 6.2 页面关系

```text
首次启动
  -> 权限说明
  -> 选择完整或部分照片访问
  -> 选择体检范围
  -> 扫描进度
  -> 体检报告

体检报告
  -> 快速整理
      -> 精确重复组
      -> 相似照片组
      -> 旧截图
      -> 模糊/空白候选
      -> 删除确认摘要
      -> 系统删除授权
      -> 删除结果
  -> 查看某一分类全部内容
  -> 修改扫描范围
  -> 隐私证明

记录
  -> 某次清理详情
  -> 失败项重试（仍可访问时）
```

---

## 7. 核心概念和术语

### 7.1 候选类型

| 类型 | 定义 | 默认行为 |
|---|---|---|
| 完全重复 | 文件大小和 SHA-256 均相同 | 除推荐保留项外可预选 |
| 相似照片 | 感知哈希接近，但文件内容不同 | 不预选，人工选择 |
| 可能拍摄失败 | 模糊、空白或严重异常 | 不预选，人工确认 |
| 旧截图 | 截图且超过用户设置的期限 | 不预选，人工确认 |
| 文档/票据 | 本地标签判断为文字、票据、二维码等 | 自动保护 |
| 未分类 | 没有足够依据给出建议 | 不进入快速整理 |

### 7.2 风险等级

内部枚举建议使用：

```kotlin
enum class ReviewRisk {
    LOW,
    NEEDS_REVIEW,
    PROTECTED
}
```

面向用户的显示：

| 内部值 | 用户文案 | 颜色/图标 | 含义 |
|---|---|---|---|
| LOW | 低风险候选 | 蓝色 + 对勾盾牌 | 有较强客观依据，但仍由用户确认 |
| NEEDS_REVIEW | 需要确认 | 黄色 + 眼睛 | 判断有主观性，不能自动选择 |
| PROTECTED | 已保护 | 绿色 + 锁 | 默认禁止加入删除列表 |

不得使用单纯红/黄/绿颜色表达状态；必须同时包含文字和图标。

### 7.3 审查状态

```kotlin
enum class ReviewDecision {
    UNREVIEWED,
    KEEP,
    DELETE,
    PROTECT
}
```

- `KEEP`：本轮保留，未来重新扫描时不应立即再次推荐；
- `DELETE`：加入当前删除清单，尚未代表系统已删除；
- `PROTECT`：长期保护，除非用户手动取消；
- 系统删除成功后才从本地有效照片集合移除。

---

## 8. 功能详细设计

## 8.1 照片体检报告

### 目标

让用户在 10 秒内理解：发现了什么、风险如何、能做什么、预计可释放多少空间。

### 报告结构

```text
照片体检
已检查 2,341 张 · 更新于今天 14:32

预计可整理 1.6 GB
以上为候选文件大小，实际释放以系统删除结果为准

[开始快速整理]

低风险候选
  完全重复       12 张 · 328 MB

需要确认
  相似照片       31 组 · 624 MB
  旧截图         86 张 · 412 MB
  可能拍摄失败   17 张 · 108 MB

已保护
  文档和票据      9 张
  用户保护       14 张

扫描范围：DCIM/Camera、Pictures/Screenshots
[修改范围] [重新体检]
```

### 交互规则

- 主 CTA 始终只有一个：“开始快速整理”；
- “重新体检”和“修改范围”为次级按钮；
- 点击分类卡进入对应筛选页；
- 扫描时间、访问级别和范围必须可见；
- 部分照片权限下显示持久提示：“报告仅覆盖系统授权的照片”；
- 无结果时不显示空白仪表盘，显示“当前范围没有发现整理候选”并提供修改范围按钮；
- 报告加载超过 300ms 显示骨架屏，不显示跳动的空数字。

### 计算规则

- “预计可整理空间”只能统计当前建议删除的非推荐保留项；
- 相似组中至少保留一张，不能把整组大小都算作可释放；
- 保护项不计入预计释放；
- 同一照片只能归入一个主类别，避免空间重复计算；
- 类别优先级：保护 > 完全重复 > 相似 > 旧截图 > 拍摄失败 > 未分类。

---

## 8.2 精确重复照片检测

### 目标

提供比视觉相似更确定的“完全重复”结论，形成可信度最高的整理入口。

### 算法流程

1. 只处理当前授权和所选目录范围内的照片；
2. 先按文件大小分组，大小只有一份的文件不计算哈希；
3. 对同大小候选以流式读取计算 SHA-256，禁止一次性载入完整文件；
4. 以 `size + sha256` 作为完全重复组键；
5. 同组数量大于 1 才生成重复组；
6. 计算过程运行在 `Dispatchers.IO`，支持协程取消；
7. 缓存哈希，只有 URI、大小或修改时间变化时重新计算；
8. 无法读取的照片不进入完全重复组，记录为分析失败但不建议删除。

### 推荐保留规则

完全重复文件的像素和内容相同，推荐保留项按以下顺序选择：

1. 用户已保护或已确认保留；
2. 位于相机主目录；
3. 不位于缓存、下载或应用临时目录；
4. 路径层级更稳定、文件名更接近相机原始命名；
5. 日期更早；
6. URI 字典序作为最终稳定排序。

必须向用户显示推荐原因，例如：“推荐保留相机目录中的原始副本”。

### 安全规则

- 每组至少保留一张；
- 用户不能把整组全部加入删除清单，除非先明确选择“全部删除”，并再次确认；
- 受保护照片不能被预选；
- SHA-256 相同且文件大小相同才能标记“完全重复”；
- dHash 结果只能标记“相似”，不能使用“重复”字样。

---

## 8.3 风险分级和保护规则

### 默认保护条件

满足任意条件即进入 `PROTECTED`：

- 用户手动保护；
- 用户曾明确选择保留，且分析版本未发生重大变化；
- 分类为文档、票据、文字、条码或二维码；
- 拍摄时间在最近 30 天内（允许用户关闭或调整）；
- 位于用户指定的保护目录；
- 当前媒体访问不完整，无法确认上下文；
- 文件读取或分析失败。

### 低风险条件

第一版只有以下候选允许标记 `LOW`：

- 完全重复组中的非推荐保留副本；
- 确认无法正常解码且文件不是零时长临时写入状态的损坏图片（若本周不实现损坏检测，则不包含）。

空白和模糊照片存在误判，第一版仍归为 `NEEDS_REVIEW`。

### 设置项

```text
保护规则

[开] 保护最近 30 天照片
[开] 保护文档、票据和二维码
[开] 保护我明确保留过的照片
保护目录（2） >

旧截图期限：90 天
```

保护设置应提供解释文本，不使用只有标题的开关。

---

## 8.4 快速整理会话

### 目标

把“面对数百张候选”的压力转化为一个可随时结束的小任务。

### 会话生成

默认一轮最多包含 20 个决策单元：

- 一个完全重复组 = 1 个决策单元；
- 一个相似组 = 1 个决策单元；
- 一张旧截图或拍摄失败候选 = 1 个决策单元。

排序优先级：

1. 低风险且预计释放空间大的完全重复组；
2. 预计释放空间大的相似组；
3. 最旧的截图；
4. 高置信度模糊或空白候选；
5. 其他需要确认项。

### 会话首页

```text
本轮快速整理

20 个决定
预计 3-5 分钟
最多可释放约 486 MB

8 组完全重复
6 组相似照片
6 张旧截图

[开始]
[调整本轮内容]
```

时间只能作为估算，不显示强制倒计时。

### 单组审查

```text
3 / 20                     [稍后处理]

完全重复 · 低风险
3 个完全相同的文件

[主图/缩略图区域]

推荐保留
DCIM/Camera/IMG_1024.jpg
推荐原因：位于相机原始目录

另外 2 个副本可释放 18.4 MB

[保留全部]
[选择其他保留项]
[保留推荐项，整理其余副本]
```

### 进度与退出

- 顶部显示当前进度，但不制造必须完成的压力；
- 每个操作立即保存到 Room；
- 系统返回、应用被杀或旋转屏幕后可以继续；
- “稍后处理”不会被当作保留或删除；
- 会话结束前只生成删除清单，不立即调用系统删除请求。

### 会话完成摘要

```text
本轮已完成

检查 20 项
准备整理 27 张 · 486 MB
保留 18 张
跳过 3 项

[查看删除清单]
[完成，稍后删除]
```

进入系统删除前，必须先显示所有待删除项的摘要。

---

## 8.5 相似照片对比和“推荐保留”

### 第一版评分

在当前能力范围内，可以组合以下指标：

- 是否被模糊检测命中；
- 像素面积；
- 文件大小；
- 是否位于相机主目录；
- 是否被用户保护或明确保留；
- 拍摄/添加时间；
- dHash 与组中心的距离。

建议生成可解释的 `qualityScore`，但 UI 不直接显示抽象总分。UI 显示理由：

- “清晰度更好”；
- “分辨率更高”；
- “位于相机目录”；
- “你曾选择保留”；
- “与本组其他照片最接近”。

如果没有明显领先项，则显示“本组没有明确推荐，请手动选择”，禁止强行给出最佳照片。

### 对比界面

- 手机竖屏：主图在上，候选缩略图横向列表在下；
- 横屏/平板：两张并排对比；
- 点击候选切换对比对象；
- P1 支持双击放大；P2 支持两张同步缩放；
- 显示分辨率、文件大小、目录、时间和检测理由；
- 关键操作必须有文字按钮，不能只依赖左右滑动；
- 滑动可以作为快捷方式，但第一次进入应显示一次简短提示。

### 分组风险

当前 dHash 阈值只能作为候选聚类。必须避免：

- 把不同构图但颜色接近的图片强行归为重复；
- 在一个巨大组中进行两两比较造成性能问题；
- 因相似就默认选择删除。

建议按拍摄时间窗口先分桶，再使用 dHash：例如相隔不超过 10 分钟的照片优先构成相似组；截图和下载图片可使用独立规则。

---

## 8.6 旧截图专项整理

### 范围

第一版只依赖已有截图识别和日期，不引入 OCR。

### 分类入口

```text
旧截图

超过 180 天       42 张 · 236 MB
90-180 天          31 张 · 154 MB
30-90 天           13 张 · 47 MB
最近 30 天         已保护
```

### 规则

- 默认期限为 90 天；
- 最近 30 天截图默认保护；
- 用户可以选择 30/90/180 天；
- 文档、票据、文字或二维码命中的截图优先进入保护；
- 旧截图仍为“需要确认”，不得自动删除；
- 按月份分组便于快速批量选择；
- 每批操作显示数量和大小。

---

## 8.7 删除确认、系统授权和结果核验

### 状态定义

必须明确区分：

```text
建议删除 -> 用户加入删除清单 -> 系统请求已发起 -> 系统返回 -> URI 核验 -> 成功/失败
```

系统返回 `RESULT_OK` 不等于所有 URI 已实际删除，现有逐 URI 核验逻辑应保留。

### 删除清单页

```text
确认整理 27 张照片

完全重复      16 张 · 320 MB
相似照片       7 张 · 128 MB
旧截图         4 张 · 38 MB

已确认每组至少保留一张
受保护照片不会包含在本清单中

[查看全部 27 张]

[请求系统删除]
[返回修改]
```

### 删除模式

产品目标应支持两种模式，但必须根据设备实际兼容性分阶段实现：

1. **移入系统回收站（推荐默认）**：可恢复，但不会立即释放全部空间；
2. **永久删除**：立即释放空间，无法撤销，作为高级模式。

如果下一周只保留当前永久删除实现，则必须：

- 明确写“永久删除，无法撤销”；
- 二次确认；
- 不预选 `NEEDS_REVIEW` 和 `PROTECTED`；
- 完成逐 URI 核验；
- 在 Redmi 17 Pro / Android 16 上进行真机测试后才发布。

### 删除完成页

```text
整理完成

成功删除 25 张
实际释放约 452 MB

2 张未能删除
系统中仍可找到这些照片，已恢复到待审查列表

[查看失败项]
[返回体检报告]
```

实际释放空间以“删除前已核验文件大小之和”为近似值，文案使用“约”；失败项不能计入。

---

## 8.8 清理记录

### 记录内容

每次系统删除请求完成后保存：

- 请求时间；
- 请求数量和大小；
- 成功数量和大小；
- 失败数量；
- 按候选类型的成功数量；
- 删除模式（回收站/永久）；
- 扫描范围摘要；
- 应用版本和分析规则版本。

不保存照片副本，不上传记录。

### 记录列表

```text
7 月 15 日 20:41
成功整理 25 张 · 约 452 MB
2 张失败

7 月 12 日 18:08
成功整理 8 张 · 约 126 MB
```

首页“累计释放空间”只能使用成功记录累计值，不能继续使用当前“建议清理大小”冒充已释放空间。

---

## 8.9 隐私证明

### 页面内容

```text
隐私证明

网络权限              未申请
照片上传              不会上传
账号系统              无
广告与分析 SDK         无
照片识别              仅在此设备运行
保存的数据            URI、大小、分类和审查决定

你可以随时：
[清除本地分析数据]
[打开系统权限设置]
[查看开源代码]
```

### 实现要求

- “网络权限未申请”应从 Manifest 事实出发，不使用无法验证的营销文案；
- 清除本地分析数据不删除原始照片；
- 操作前明确说明只清理 Room/DataStore；
- 如果未来增加任何联网能力，本页必须同步更新。

---

## 9. 首次使用和权限体验

### 9.1 首次启动三屏以内

**屏 1：价值和隐私**

```text
在本地完成照片体检
照片不会离开设备，也不需要账号。
[继续]
```

**屏 2：权限解释**

```text
选择 PhotoCleaner 可以检查的照片

完整访问：生成完整体检报告
部分访问：只检查你授权的照片

[选择照片访问范围]
```

**屏 3：扫描范围**

```text
从哪里开始？

[相机照片]
[截图]
[下载图片]
[自定义目录]

[开始首次体检]
```

### 9.2 权限原则

- 在系统权限弹窗前解释用途；
- 用户选择部分访问时不阻塞使用；
- 不反复弹窗逼迫完整授权；
- 设置页提供重新授权入口；
- 报告必须持续显示当前访问级别；
- 权限变化后刷新目录和报告，不错误删除 Room 记录。

---

## 10. 视觉设计系统

### 10.1 设计性格

关键词：**可信、克制、清晰、本地、专业、友好**。

避免：

- 夸张的霓虹、扫描雷达和“病毒清理”视觉；
- 大面积危险红色制造焦虑；
- 过多玻璃卡片导致信息层级不清；
- 用动画掩盖长时间处理；
- 把所有候选显示成危险项。

### 10.2 色彩角色

继续使用 Material 3 语义色，不在组件中随意新增硬编码颜色：

- 主操作：稳定蓝色；
- 成功/保护：绿色；
- 需要确认：琥珀色；
- 永久删除：红色，仅用于最终危险操作；
- 背景：深色中性表面，同时应预留未来浅色模式；
- 正文对比度至少 4.5:1；
- 图表和风险状态不能只靠颜色区分。

### 10.3 排版与空间

- 使用系统 Roboto/Material 字体体系，不额外下载字体；
- 正文优先使用 `bodyLarge/bodyMedium`，避免过多小于 14sp 的说明；
- 采用 8dp 间距网格；
- 卡片内部至少 16dp；
- 关键数字使用等宽数字特性或稳定宽度，避免扫描时布局跳动；
- 长文件名允许换行或进入详情查看，不在关键证据处强制省略。

### 10.4 动效

- 点击后 100ms 内提供视觉反馈；
- 微交互 150-300ms；
- 只使用位移、缩放和透明度表达层级；
- 扫描过程使用确定进度，无法确定时才使用不确定进度；
- 尊重系统“移除动画”或动画时长设置；
- 动画不能阻塞返回、取消或停止扫描。

### 10.5 触摸与无障碍

- 所有触控目标至少 48x48dp；
- 相邻危险操作和普通操作至少间隔 8dp；
- 图标按钮必须有 `contentDescription`；
- 照片卡必须读出“文件名、风险等级、建议理由、当前选择状态”；
- 删除、保留和保护动作提供可见按钮，不能只靠滑动；
- TalkBack 顺序与视觉顺序一致；
- 字体放大到 200% 时核心操作不被截断；
- 横屏和大屏采用并排布局，小屏不出现横向溢出。

---

## 11. 内容与文案规范

### 11.1 推荐用语

| 不推荐 | 推荐 |
|---|---|
| 垃圾照片 | 整理候选 |
| AI 判定 | 本地检测建议 |
| 一键清理 | 快速整理 |
| 最佳照片 | 推荐保留 |
| 可安全删除 | 低风险候选 |
| 已释放（删除前） | 预计可整理 |
| 删除成功（仅 RESULT_OK） | 系统已确认，正在核验 |

### 11.2 错误文案格式

错误必须说明“发生了什么 + 数据是否安全 + 下一步”：

```text
2 张照片未能删除。它们仍保留在系统相册中，已恢复到待审查列表。你可以稍后重试。
```

禁止只显示：

```text
操作失败
未知错误
```

### 11.3 数字口径

- “扫描照片数”：MediaStore 在当前权限和目录条件下实际返回的数量；
- “已分析数”：完成本地分类并保存结果的数量；
- “候选数”：进入报告的唯一照片数量；
- “预计可整理空间”：当前候选可删除项大小之和；
- “实际释放空间”：本次核验删除成功项的原大小之和，以“约”显示；
- “累计释放空间”：清理历史中成功项累计值。

---

## 12. 技术设计

## 12.1 架构原则

保留现有 MVVM + Clean Architecture：

```text
Compose UI
  -> ViewModel / UiState
  -> UseCase
  -> Repository interface
  -> Room / MediaStore / local classifier
```

- Android Framework 类型不进入 domain repository；
- SHA-256、风险评估和推荐规则拆成独立、可测试的纯 Kotlin 组件；
- UI 不直接解释算法原始字段，统一通过展示模型输出文案资源 ID；
- 长任务支持取消，扫描服务始终在 `finally` 中停止；
- 部分照片权限不能触发数据库全量清理。

## 12.2 推荐领域模型

```kotlin
enum class CandidateType {
    EXACT_DUPLICATE,
    SIMILAR,
    OLD_SCREENSHOT,
    POSSIBLE_FAILED_SHOT,
    PROTECTED_DOCUMENT
}

enum class ReviewRisk { LOW, NEEDS_REVIEW, PROTECTED }

enum class ReviewDecision { UNREVIEWED, KEEP, DELETE, PROTECT }

data class ReviewCandidate(
    val photoId: Long,
    val groupId: String?,
    val type: CandidateType,
    val risk: ReviewRisk,
    val decision: ReviewDecision,
    val reasonCodes: List<String>,
    val recommendedKeep: Boolean,
    val estimatedReclaimableBytes: Long
)
```

原因使用稳定 code 存储，例如：

```text
exact_duplicate
camera_original_location
higher_resolution
less_blurry
old_screenshot
document_detected
recent_photo
user_protected
analysis_failed
```

UI 根据 code 读取本地化字符串，禁止把中文文案直接存进数据库作为长期结构字段。

## 12.3 Room 方案

建议数据库从 v4 升级到 v5。为了控制一周范围，可继续扩展 `photos` 表并增加少量新表。

### `photos` 新字段

| 字段 | 类型 | 默认值 | 用途 |
|---|---|---|---|
| `contentHash` | TEXT? | null | SHA-256 精确重复检测 |
| `hashSourceSize` | INTEGER | 0 | 判断哈希是否失效 |
| `hashSourceModified` | INTEGER | 0 | 判断哈希是否失效 |
| `reviewDecision` | TEXT | UNREVIEWED | 用户当前决定 |
| `isProtected` | INTEGER | 0 | 用户或规则保护 |
| `analysisVersion` | INTEGER | 1 | 规则变化后的重算依据 |
| `lastAnalyzedAt` | INTEGER | 0 | 报告时间和增量分析 |

### `cleanup_sessions`

| 字段 | 用途 |
|---|---|
| `id` | UUID 字符串主键 |
| `createdAt/completedAt` | 会话时间 |
| `requestedCount/requestedBytes` | 系统请求前数据 |
| `deletedCount/deletedBytes` | URI 核验成功数据 |
| `failedCount` | 删除失败数 |
| `deleteMode` | TRASH/PERMANENT |
| `appVersion/analysisVersion` | 可追溯性 |

### `cleanup_session_items`

保存 `sessionId + photoId + candidateType + requested + deleted + originalSize`。历史详情不应依赖照片行仍然存在。

### 迁移要求

- 提供明确的 `MIGRATION_4_5`；
- 更新 `PhotoDatabaseMigrations.ALL`；
- 导出 v5 schema；
- 使用 Room Migration Test 验证 v4 -> v5；
- 不使用破坏性迁移掩盖缺失字段；
- 迁移后现有照片默认 `UNREVIEWED`，现有 `KEEP` 分类可转换为受保护或已保留状态。

## 12.4 精确哈希组件

建议接口：

```kotlin
interface PhotoContentHasher {
    suspend fun sha256(uri: String): String?
}
```

实现要求：

- `ContentResolver.openInputStream()`；
- 64-256 KiB 缓冲区流式读取；
- 每个循环检查协程取消；
- 读取失败返回 null 并记录分析状态，不将其当成重复；
- 同时运行的哈希任务限制在 2 个，避免低端设备 I/O 抖动；
- 首次报告可以先展示现有分类，精确重复计算完成后增量更新报告。

## 12.5 风险评估组件

```kotlin
interface ReviewRiskEvaluator {
    fun evaluate(photo: PhotoAnalysisContext): RiskResult
}
```

它应为纯 Kotlin 规则引擎，输入包括候选类型、时间、目录、分析结果和用户设置，输出风险与原因 code。规则顺序必须确定且有测试。

## 12.6 推荐保留组件

```kotlin
interface KeepRecommendationEngine {
    fun recommend(group: List<PhotoAnalysisContext>): KeepRecommendation?
}
```

输出：推荐 photo ID、理由列表和是否存在明显领先项。规则相同输入必须得到相同输出，避免 UI 每次打开推荐不同照片。

## 12.7 会话状态

快速整理 ViewModel 不应把全部流程只保存在内存：

- 生成会话时写入 Room；
- 每次决定后立即保存；
- UI state 只保存当前页展示模型；
- 重启应用后查询未完成会话并提供继续；
- 系统删除授权期间设置 `REQUEST_IN_FLIGHT`，禁止重复发起请求；
- 回调后执行 URI 核验，再完成会话。

---

## 13. 性能与资源预算

### 13.1 目标

- 目录报告数字必须来自数据库聚合或单次查询，避免 Compose 重组时重复遍历全库；
- 10,000 张照片情况下，列表使用 Lazy 组件，不一次加载全分辨率位图；
- 分类位图继续下采样到合理尺寸；
- 精确哈希仅对同大小候选计算；
- 同时分类任务维持受控并发；
- 长扫描在前台服务中运行，并可停止；
- App 返回前台时不重复启动相同任务。

### 13.2 可接受标准

- 首页已有缓存报告时 500ms 内出现主要内容；
- 快速整理切换候选时先显示缩略图，避免明显白屏；
- 用户点击停止后 1 秒内 UI 进入停止状态；
- 处理过程中内存不随照片数量持续线性增长；
- 同一照片的 SHA-256 在源数据未变化时不重复计算。

---

## 14. 状态和异常设计

每个主要页面都必须覆盖：

- 初始状态；
- 加载状态；
- 有数据状态；
- 空状态；
- 部分权限状态；
- 权限被撤销；
- 扫描取消；
- 单照片分析失败；
- 系统删除请求创建失败；
- 用户取消系统删除；
- 系统返回成功但部分 URI 仍存在；
- 媒体在审查期间被其他应用删除；
- SD 卡在扫描期间拔出；
- 进程被杀后恢复。

异常原则：单张照片失败不终止整轮；范围或权限失效时停止危险的数据同步操作，但保留已完成结果。

---

## 15. 本地指标与产品验证

本版本不增加联网埋点。可在设备本地统计并在调试页或记录页查看：

- 首次体检是否完成；
- 每轮候选数量；
- 快速整理完成率；
- 每类候选的保留/删除/跳过比例；
- 用户修改推荐保留项的比例；
- 删除请求成功率；
- URI 核验失败率；
- 实际释放空间；
- 用户手动保护数量。

这些数据帮助调整算法，但不得包含图片内容、文件名或可导出的个人路径。正式版默认不导出；如提供诊断导出，必须由用户主动触发并预览内容。

### 成功判断

下周版本不以“扫描出更多垃圾”为成功，而以以下结果判断：

- 用户能解释为什么某张照片被推荐；
- 完全重复组无误报；
- 保护项不会进入默认删除清单；
- 一轮 20 项整理可以中断并恢复；
- 删除数量与实际核验结果一致；
- 首页的预计空间、实际空间和累计空间口径正确。

---

## 16. 验收标准

### 16.1 体检报告

- [ ] 报告只统计当前权限和所选目录范围；
- [ ] 同一照片不会被多个类别重复计算空间；
- [ ] 部分访问状态始终可见；
- [ ] 空状态提供下一步操作；
- [ ] “预计可整理”没有被写成“已释放”。

### 16.2 精确重复

- [ ] 相同字节的文件进入同组；
- [ ] 视觉相同但重新压缩的文件不进入“完全重复”组；
- [ ] 每组至少保留一张；
- [ ] 哈希读取异常不会生成低风险候选；
- [ ] 源文件未变化时复用哈希；
- [ ] 任务可取消且不会遗留前台服务。

### 16.3 风险和保护

- [ ] 文档、票据和二维码默认保护；
- [ ] 最近照片按规则保护；
- [ ] 用户保护状态跨重启保留；
- [ ] 保护项不能被批量预选；
- [ ] 状态同时使用图标、文字和颜色表达；
- [ ] 每个候选至少显示一个理由。

### 16.4 快速整理

- [ ] 默认最多 20 个决策单元；
- [ ] 每次选择立即持久化；
- [ ] 返回或进程恢复后可继续；
- [ ] 支持跳过且不改变照片分类；
- [ ] 最终删除前显示完整摘要；
- [ ] 系统请求处理中不能重复提交。

### 16.5 删除与记录

- [ ] 用户取消系统请求后所有照片恢复到可审查状态；
- [ ] 只有 URI 核验确认不存在的照片计入删除成功；
- [ ] 失败照片不计入释放空间；
- [ ] 失败照片可以查看并重试；
- [ ] 清理记录在照片行删除后仍保留必要摘要；
- [ ] Redmi 17 Pro / Android 16 真机通过单张和批量删除测试。

### 16.6 无障碍

- [ ] 关键按钮触控目标至少 48dp；
- [ ] 图标按钮均有无障碍名称；
- [ ] TalkBack 能读出候选、理由和选择状态；
- [ ] 所有滑动操作都有可见替代按钮；
- [ ] 字体放大 200% 后仍可完成主流程；
- [ ] 减少动画设置生效。

---

## 17. 测试计划

### 17.1 单元测试

- SHA-256 完全重复分组；
- 同大小不同内容不分组；
- 哈希缓存失效条件；
- 风险规则优先级；
- 保护规则覆盖低风险规则；
- 推荐保留稳定排序；
- 报告空间去重；
- 会话最多 20 个决策单元；
- 删除成功/部分成功/全部失败统计；
- 部分媒体权限下不清理不可见数据库记录。

### 17.2 Room 测试

- v4 -> v5 迁移；
- 旧 `KEEP` 数据转换；
- 会话和项目级联/保留策略；
- 删除照片行后清理历史仍可读取；
- 索引存在且常用聚合查询可执行。

### 17.3 UI 测试

- 体检报告有数据、空状态和部分权限状态；
- 快速整理保存决定；
- 保护项按钮禁用或要求先取消保护；
- 删除摘要数字准确；
- 系统删除取消后恢复；
- TalkBack 节点具有正确标签；
- 手机竖屏、横屏和平板宽度布局。

### 17.4 真机矩阵

最低要求：

| 设备/系统 | 重点 |
|---|---|
| Redmi 17 Pro / Android 16 | 删除、部分权限、前台扫描、HyperOS 相册同步 |
| Pixel 或 AOSP Android 16 | 标准 MediaStore 行为 |
| Android 14 设备/模拟器 | 部分照片访问 |
| Android 11 设备/模拟器 | 最低版本和存储兼容 |

每台设备验证：单张删除、20 张批量删除、用户取消、部分成功、所选目录计数、后台扫描和进程恢复。

---

## 18. 下周实施计划

### Day 1：数据基础和规则

- 冻结领域模型和原因 code；
- 设计并实现 Room v5 migration；
- 增加分析版本、保护和审查状态；
- 编写迁移测试；
- 实现 SHA-256 组件及单元测试。

**当日退出条件：** 数据迁移通过，重复检测可在纯用例测试中稳定工作。

### Day 2：体检聚合与风险评估

- 实现候选类型聚合；
- 实现风险和保护规则；
- 实现推荐保留规则；
- 修正预计释放空间口径；
- 为所有规则增加单元测试。

**当日退出条件：** 给定固定照片集合，报告、风险和推荐结果完全可预测。

### Day 3：体检报告首页

- 改造 Home/体检页面 UiState；
- 实现报告卡片、权限/范围提示和主要 CTA；
- 加入隐私证明页面；
- 完成加载、空、错误和部分权限状态；
- 做字体放大和 TalkBack 初检。

**当日退出条件：** 用户可以从首页理解报告并进入对应候选。

### Day 4：快速整理与相似对比

- 实现持久化整理会话；
- 实现单组审查、跳过、保留、保护和删除决定；
- 实现完成摘要；
- 改造相似组推荐文案；
- 时间允许时增加并排对比。

**当日退出条件：** 20 项会话可暂停、恢复并生成准确删除清单。

### Day 5：删除结果与记录

- 将删除清单接入现有系统请求；
- 保留逐 URI 重试核验；
- 保存成功/失败记录和实际空间；
- 实现失败项恢复；
- 增加删除流程回归测试。

**当日退出条件：** 单张、批量、取消和部分失败都有一致状态。

### Day 6：真机、UI 和无障碍修复

- Redmi 17 Pro / Android 16 真机回归；
- Android 14 部分权限回归；
- 修复横屏、大字体和 TalkBack；
- 核对所有数字口径；
- 检查文案、危险操作和触控目标。

### Day 7：发布门禁和文档

- 更新 README 和版本说明；
- 在 `C:\temp\PhotoCleaner` 同步干净源码；
- 执行单元测试、Lint、Debug 和 R8 Release 构建；
- 检查 APK 版本名；
- 生成签名发布包（若签名环境可用）；
- 完成最终人工验收清单。

---

## 19. 开发任务拆分建议

建议按以下独立提交组织，方便回滚和审查：

1. `Add photo analysis and cleanup session schema`
2. `Add exact duplicate detection and risk rules`
3. `Build photo health report experience`
4. `Add resumable quick review sessions`
5. `Add explainable similar photo comparison`
6. `Record verified cleanup outcomes`
7. `Polish accessibility and update documentation`

每个提交都应包含对应测试，不要把数据库、算法、UI 和文档压成一个巨大提交。

---

## 20. 发布门禁

### 代码和数据

- [ ] 所有迁移都有测试；
- [ ] 不新增 `INTERNET` 权限；
- [ ] 不新增未说明的 SDK；
- [ ] 部分权限不会导致数据误删；
- [ ] 所有长任务可取消；
- [ ] 删除成功由 URI 核验决定。

### UX

- [ ] 首页只有一个主要 CTA；
- [ ] 每个建议有理由；
- [ ] 保护项默认不会进入删除清单；
- [ ] 预计和实际空间用语严格区分；
- [ ] 危险操作有明确确认和恢复路径说明；
- [ ] TalkBack 和大字体主流程可用。

### 验证

按照仓库的 OneDrive 安全要求，只在 `C:\temp\PhotoCleaner` 执行最终验证：

```powershell
.\gradlew.bat testDebugUnitTest lintDebug assembleDebug assembleRelease
```

还需执行：

```powershell
git diff --check
```

并人工检查中文 UTF-8、版本名、APK 元数据和 Redmi 17 Pro 真机删除流程。

---

## 21. 最终交付形态

下一版本完成后，用户首次打开首页时不应该看到一个需要学习的“扫描工具”，而应该看到一个清晰的决定入口：

```text
你的照片已在本地完成体检

发现 12 张完全重复照片
31 组相似照片需要选择
9 张文档和票据已保护

预计可整理 1.6 GB

[开始快速整理]

照片不会上传，最终删除由你确认
```

这一体验是下一版本所有设计和技术决策的共同目标：

> 不是替用户武断地删除照片，而是降低理解成本、决策成本和误删风险。

